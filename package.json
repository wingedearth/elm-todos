{
  "name": "elm-hello-world",
  "version": "1.0.0",
  "scripts": {
    "build": "elm make src/Main.elm --output=elm.js",
    "dev": "elm make src/Main.elm --output=elm.js && python3 -m http.server 8000",
    "watch": "elm make src/Main.elm --output=elm.js --debug"
  },
  "devDependencies": {
    "elm": "^0.19.1-6"
  }
}